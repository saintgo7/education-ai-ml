# Module 16: 의사결정트리
# Configuration file

module:
  id: 16
  name: 의사결정트리
  english_name: Decision Trees
  topics: 엔트로피, 정보이득, 가지치기

# 학습 관련 설정
learning:
  difficulty: intermediate
  estimated_hours: 5
  prerequisites: []

# 데이터 설정
data:
  source: synthetic
  size: small
  format: csv

# 모델 설정
model:
  type: example
  architecture: baseline
  hyperparameters:
    learning_rate: 0.001
    batch_size: 32
    epochs: 10

# 평가 메트릭
metrics:
  - accuracy
  - loss
  - f1_score
