# Module 23: 계층적 클러스터링
# Configuration file

module:
  id: 23
  name: 계층적 클러스터링
  english_name: Hierarchical Clustering
  topics: 덴드로그램, 링크 기준

# 학습 관련 설정
learning:
  difficulty: intermediate
  estimated_hours: 5
  prerequisites: []

# 데이터 설정
data:
  source: synthetic
  size: small
  format: csv

# 모델 설정
model:
  type: example
  architecture: baseline
  hyperparameters:
    learning_rate: 0.001
    batch_size: 32
    epochs: 10

# 평가 메트릭
metrics:
  - accuracy
  - loss
  - f1_score
