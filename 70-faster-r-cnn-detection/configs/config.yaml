# Module 70: Faster R-CNN
# Configuration file

module:
  id: 70
  name: Faster R-CNN
  english_name: Faster R-CNN Detection
  topics: 영역 제안, 두 단계 탐지

# 학습 관련 설정
learning:
  difficulty: intermediate
  estimated_hours: 5
  prerequisites: []

# 데이터 설정
data:
  source: synthetic
  size: small
  format: csv

# 모델 설정
model:
  type: example
  architecture: baseline
  hyperparameters:
    learning_rate: 0.001
    batch_size: 32
    epochs: 10

# 평가 메트릭
metrics:
  - accuracy
  - loss
  - f1_score
