# Module 75: 깊이 추정
# Configuration file

module:
  id: 75
  name: 깊이 추정
  english_name: Depth Estimation
  topics: 3D 재구성, 스테레오 비전

# 학습 관련 설정
learning:
  difficulty: intermediate
  estimated_hours: 5
  prerequisites: []

# 데이터 설정
data:
  source: synthetic
  size: small
  format: csv

# 모델 설정
model:
  type: example
  architecture: baseline
  hyperparameters:
    learning_rate: 0.001
    batch_size: 32
    epochs: 10

# 평가 메트릭
metrics:
  - accuracy
  - loss
  - f1_score
