# Module 91: GAN 기초
# Configuration file

module:
  id: 91
  name: GAN 기초
  english_name: GAN Fundamentals
  topics: 생성기, 판별기, 적대적 훈련

# 학습 관련 설정
learning:
  difficulty: intermediate
  estimated_hours: 5
  prerequisites: []

# 데이터 설정
data:
  source: synthetic
  size: small
  format: csv

# 모델 설정
model:
  type: example
  architecture: baseline
  hyperparameters:
    learning_rate: 0.001
    batch_size: 32
    epochs: 10

# 평가 메트릭
metrics:
  - accuracy
  - loss
  - f1_score
