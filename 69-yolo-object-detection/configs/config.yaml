# Module 69: YOLO 객체 탐지
# Configuration file

module:
  id: 69
  name: YOLO 객체 탐지
  english_name: YOLO Object Detection
  topics: 실시간 탐지, 박스 회귀

# 학습 관련 설정
learning:
  difficulty: intermediate
  estimated_hours: 5
  prerequisites: []

# 데이터 설정
data:
  source: synthetic
  size: small
  format: csv

# 모델 설정
model:
  type: example
  architecture: baseline
  hyperparameters:
    learning_rate: 0.001
    batch_size: 32
    epochs: 10

# 평가 메트릭
metrics:
  - accuracy
  - loss
  - f1_score
